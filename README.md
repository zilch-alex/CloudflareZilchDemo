Follow instructions to setup React Native

https://reactnative.dev/docs/environment-setup

To install:

```
npm install

cd ios && pod install
```

To run:

Open `ios/CloudflareZilchDemo.xcworkspace` in Xcode.

Run project
